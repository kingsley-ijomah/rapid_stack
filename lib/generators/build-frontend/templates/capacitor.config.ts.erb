import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '<%= projectName %>',
  // webDir: 'www/frontend/browser'
  webDir: 'www/browser'
};

export default config; 